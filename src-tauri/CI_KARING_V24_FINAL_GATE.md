# Karing v2.5.4-karing.24 final same-SHA gate

This file is intentionally kept under `src-tauri/` so the final merge-candidate commit reruns repository Rust formatting and Clippy checks against the complete current source tree.

The release candidate is accepted only when the same commit also passes the Windows Mobile Hotspot v24 lifecycle/data-plane gate, the patched Mihomo WFP v21 gate, workflow semantic lint, frontend checks, and pull-request review.
