name: Frontend Check
run-name: Audit Karing 4 candidate with setup report

on:
  push:
    branches:
      - dev
    paths:
      - '.github/workflows/frontend-check.yml'
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

env:
  HUSKY: 0

concurrency:
  group: karing4-readonly-audit
  cancel-in-progress: true

jobs:
  frontend:
    name: Audit feature candidate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature candidate
        id: checkout
        uses: actions/checkout@v7
        with:
          ref: feature/karing-style-diversion
          fetch-depth: 0

      - name: Install pnpm
        id: pnpm_setup
        continue-on-error: true
        uses: pnpm/action-setup@v6
        with:
          run_install: false

      - name: Install Node.js
        id: node_setup
        continue-on-error: true
        uses: actions/setup-node@v6
        with:
          node-version: '24.18.0'
          cache: pnpm

      - name: Install dependencies
        id: install
        if: steps.pnpm_setup.outcome == 'success' && steps.node_setup.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm install --frozen-lockfile 2>&1 | tee /tmp/install.log

      - name: Check formatting
        id: format
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm format:check 2>&1 | tee /tmp/format.log

      - name: Audit mature routing UI
        id: ui
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          {
            set -euo pipefail
            test -f src/components/base/overflow-reveal.tsx
            grep -q "overflowX: 'auto'" src/components/base/overflow-reveal.tsx
            grep -q 'ResizeObserver' src/components/base/overflow-reveal.tsx
            grep -q 'softScrollAreaSx' src/components/rule/diversion-manager/detected-programs-panel.tsx
            grep -q 'OverflowReveal' src/components/rule/diversion-manager/project-panel.tsx
            grep -q 'OverflowReveal' src/components/rule/diversion-manager/action-picker.tsx
            grep -q 'OverflowReveal' src/components/routing/routing-relations-panel.tsx
            grep -q 'OverflowReveal' src/components/routing/focused-policy-panel.tsx
            grep -q 'useConnectionData({ enabled: open })' src/components/connection/connection-detail.tsx
            grep -q 'isMihomoFakeIp' src/components/connection/connection-rule-assistant.tsx
            grep -q 'MAX_ATTRIBUTION_ITEMS' src/services/process-connections.ts
            echo '[pass] routing UI and overflow reveal audit'
          } 2>&1 | tee /tmp/ui.log

      - name: Run ESLint
        id: lint
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm lint 2>&1 | tee /tmp/lint.log

      - name: Run TypeScript check
        id: typecheck
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm typecheck 2>&1 | tee /tmp/typecheck.log

      - name: Run Knip report
        id: knip
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm knip:report 2>&1 | tee /tmp/knip.log

      - name: Build web application
        id: build
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm run web:build 2>&1 | tee /tmp/build.log

      - name: Publish audit report to PR
        if: always()
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PNPM_SETUP: ${{ steps.pnpm_setup.outcome }}
          NODE_SETUP: ${{ steps.node_setup.outcome }}
          INSTALL: ${{ steps.install.outcome }}
          FORMAT: ${{ steps.format.outcome }}
          UI: ${{ steps.ui.outcome }}
          LINT: ${{ steps.lint.outcome }}
          TYPECHECK: ${{ steps.typecheck.outcome }}
          KNIP: ${{ steps.knip.outcome }}
          BUILD: ${{ steps.build.outcome }}
        run: |
          set -euo pipefail
          candidate=$(git rev-parse HEAD)
          report=/tmp/audit-report.md
          {
            echo '<!-- karing4-frontend-audit -->'
            echo '## Karing 4 前端审查报告'
            echo
            echo "候选提交：\`$candidate\`"
            echo "Action：https://github.com/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
            echo
            echo '| 检查 | 结果 |'
            echo '| --- | --- |'
            echo "| pnpm 环境 | $PNPM_SETUP |"
            echo "| Node 环境 | $NODE_SETUP |"
            echo "| 依赖安装 | $INSTALL |"
            echo "| Biome | $FORMAT |"
            echo "| 成熟化 UI 审查 | $UI |"
            echo "| ESLint | $LINT |"
            echo "| TypeScript | $TYPECHECK |"
            echo "| Knip | $KNIP |"
            echo "| Web 构建 | $BUILD |"
            for item in install format ui lint typecheck knip build; do
              value=$(printenv "$(echo "$item" | tr '[:lower:]' '[:upper:]')")
              if [ "$value" != success ]; then
                echo
                echo "<details><summary>$item 错误日志尾部</summary>"
                echo
                echo '```text'
                tail -n 80 "/tmp/$item.log" 2>/dev/null || true
                echo '```'
                echo '</details>'
              fi
            done
          } > "$report"
          gh pr comment 1 --repo "$GITHUB_REPOSITORY" --body-file "$report"

      - name: Enforce audit result
        if: always()
        shell: bash
        env:
          PNPM_SETUP: ${{ steps.pnpm_setup.outcome }}
          NODE_SETUP: ${{ steps.node_setup.outcome }}
          INSTALL: ${{ steps.install.outcome }}
          FORMAT: ${{ steps.format.outcome }}
          UI: ${{ steps.ui.outcome }}
          LINT: ${{ steps.lint.outcome }}
          TYPECHECK: ${{ steps.typecheck.outcome }}
          KNIP: ${{ steps.knip.outcome }}
          BUILD: ${{ steps.build.outcome }}
        run: |
          set -euo pipefail
          for value in "$PNPM_SETUP" "$NODE_SETUP" "$INSTALL" "$FORMAT" "$UI" "$LINT" "$TYPECHECK" "$KNIP" "$BUILD"; do
            test "$value" = success
          done
