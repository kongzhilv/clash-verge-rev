name: Karing 4 Run Probe
run-name: Karing 4 run probe (manual only)

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Explain workflow status
        run: echo 'The final audited release workflow is the source of truth.'
